{
  "name": "reflax-local-deployment",
  "version": "1.0.0",
  "description": "Local deployment utilities for ReFlax protocol",
  "scripts": {
    "deploy-local": "anvil --fork-url http://localhost:8545 & sleep 3 && forge script scripts/deployLocal.js --rpc-url http://localhost:8545 --broadcast",
    "start-server": "node scripts/addressServer.js",
    "install-deps": "npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {},
  "keywords": ["defi", "ethereum", "reflax", "local-deployment"],
  "author": "",
  "license": "MIT"
}